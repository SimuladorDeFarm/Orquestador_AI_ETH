Aquí está el reporte ejecutivo final de la fase de exploración, sintetizando toda la información proporcionada.

---

# Reporte Ejecutivo de Exploración

**Objetivo:** 10.65.184.73

## 1. Servicios Descubiertos

Se detectaron tres servicios en el objetivo:

| Puerto | Protocolo | Servicio | Versión |
| :--- | :--- | :--- | :--- |
| 21 | TCP | FTP | vsftpd 3.0.5 |
| 22 | TCP | SSH | OpenSSH 8.2p1 Ubuntu |
| 80 | TCP | HTTP | Apache httpd 2.4.41 (Ubuntu) |

## 2. Rutas y Archivos Expuestos

Se descubrieron las siguientes rutas y archivos en el servicio HTTP:

- **`/index.html`** (Código 200) - Página principal del sitio.
- **`/images/`** (Código 301/200) - Directorio con listado habilitado. Contiene:
  - `crew.jpg`
- **`/javascript/`** (Código 301/403) - Directorio cuyo acceso fue denegado.
- **`/server-status`** (Código 403) - Estado del servidor, acceso prohibido.
- **`.htaccess`**, **`.htpasswd`**, **`.hta`** (Código 403) - Archivos de configuración de Apache, acceso prohibido.

Se descubrió el siguiente archivo en el servicio FTP (puerto 21):

- **`lupin.jpg`** (en el directorio raíz del FTP)

## 3. Contenido Relevante de Páginas

- **Página principal (`/index.html`):** Se leyó el contenido de la página. Muestra una temática de "Cowboy Bebop" con diálogos entre los personajes (Spike, Jet, Ed, Faye). El contenido de la página indica que la misión es "rootear el sistema". No se encontraron comentarios HTML ocultos ni información sensible directa en el código fuente.

## 4. Flags Encontradas

**No se encontraron flags durante ninguna de las iteraciones de la fase de exploración.**

## 5. Vectores de Ataque Sugeridos para la Próxima Fase

Basándose en los hallazgos actuales, se sugieren los siguientes vectores de ataque a explotar:

1.  **FTP Anónimo (Puerto 21):** Aunque la exploración ya confirmó el acceso anónimo y se descargó un archivo (`lupin.jpg`), se debe profundizar en este vector. Se recomienda:
    - Subir un archivo (ej. un script malicioso o una clave pública SSH) para verificar si se tienen permisos de escritura.
    - Si se permite la escritura y el servidor web sirve archivos desde el directorio FTP, se podría intentar cargar un `web shell` para obtener ejecución remota de comandos.
    - Analizar el archivo `lupin.jpg` en busca de metadatos o información oculta (esteganografía) que pueda contener credenciales o pistas adicionales.

2.  **Enumeración Web Avanzada (Puerto 80):**
    - **Directorio `/javascript/`:** Este directorio devolvió un código 403 (Prohibido). Se podría intentar un ataque de *directory traversal* o *path fuzzing* para tratar de listar o acceder a su contenido. Es posible que contenga archivos JavaScript que revelen más rutas, funcionalidades ocultas o credenciales.
    - **Archivos de configuración:** Se confirmó la existencia de `.htaccess` y `.htpasswd` (aunque con acceso denegado). Se podría intentar forzar la lectura de estos archivos mediante herramientas como `curl` con cabeceras específicas o explotando vulnerabilidades de Apache si las hubiera.

3.  **Análisis de Archivos Descargados:** Los archivos `crew.jpg` (del servidor web) y `lupin.jpg` (del FTP) deben ser descargados y analizados con herramientas de esteganografía (como `steghide`, `binwalk`, `strings`) para extraer cualquier información oculta (mensajes, credenciales o incluso flags).