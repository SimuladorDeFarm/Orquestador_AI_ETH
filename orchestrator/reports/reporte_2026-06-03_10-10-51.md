# Reporte Ejecutivo Final - Fase de Exploración

## Objetivo: 10.65.184.73

## 1. Servicios Descubiertos

| Puerto | Protocolo | Servicio | Versión |
|--------|-----------|----------|---------|
| 21/tcp | TCP | FTP | vsftpd 3.0.5 |
| 22/tcp | TCP | SSH | OpenSSH 8.2p1 Ubuntu |
| 80/tcp | TCP | HTTP | Apache httpd 2.4.41 (Ubuntu) |

## 2. Rutas y Archivos Expuestos

### Port 80 (HTTP)

**Rutas principales:**
- `/` (200 OK) - Página principal del sitio web
- `/images/` (301 redirección)
- `/javascript/` (403 Forbidden)

**Archivos encontrados:**
- `index.html` (200 OK)
- `crew.jpg` (608KB) - dentro de `/images/`

**Rutas con respuesta 403 (prohibido):**
- `.htaccess`
- `.htpasswd`
- `.hta`
- `server-status`

**Fuzzing adicional (ffuf):** No se encontraron rutas adicionales a las ya listadas.

### Port 21 (FTP)
- Login anónimo exitoso (`anonymous` autenticado)
- Directorio raíz `/` - Sin permisos de listado (550 Permission denied)

## 3. Contenido Relevante de Páginas

**Página principal (`/`):**
- Sitio web con temática de **Cowboy Bebop**
- Menciona a los personajes: Spike, Jet, Ed, Faye
- El contenido contextual sugiere que el objetivo es "rootear el sistema" (propio del laboratorio de pentesting)
- No se identificaron credenciales, formularios de login, ni datos sensibles adicionales en la página

**`/images/crew.jpg`:**
- Imagen del equipo (608KB)
- No contiene metadatos relevantes o información oculta (datos EXIF no extraídos en esta fase)

## 4. Flags Encontradas

**No se encontraron flags en ninguna de las iteraciones de exploración.**

Ninguna de las rutas, archivos, servicios o contenidos revisados contenía cadenas con formato `flag{...}`, `flg{...}` o variante similar.

## 5. Vectores de Ataque Sugeridos para la Siguiente Fase

### Prioridad Alta:
1. **FTP (puerto 21) - Fuerza bruta de directorios/archivos:**
   - Dado que el login anónimo funciona pero no permite listar, probar con herramientas como `ftp` con paths específicos (ej. `/pub`, `/incoming`, `/users`, nombre de personajes del sitio).
   - Intentar descargar archivos conocidos como `.bash_history`, `.ssh/authorized_keys`, o archivos de configuración del servidor FTP.

2. **HTTP - Búsqueda de archivos de configuración sensibles:**
   - Continuar fuzzing con wordlists más especializadas (ej. `common.txt`, `directory-list-2.3-medium.txt`) enfocándose en archivos propietarios de Apache/PHP (si aplica).
   - Probar con extensiones comunes: `.php`, `.txt`, `.bak`, `.old`, `.conf`.

### Prioridad Media:
3. **SSH (puerto 22) - Enumeración de usuarios:**
   - Usar técnicas de enumeración de usuarios (ej. `ssh -o PreferredAuthentications=none` o herramientas como `hydra` para validar usuarios).
   - Si se descubren credenciales en fases posteriores, intentar acceso vía SSH.

4. **Análisis de vulnerabilidades conocidas:**
   - Verificar si `vsftpd 3.0.5` o `OpenSSH 8.2p1` tienen CVEs explotables sin autenticación (en esta versión, vsftpd 3.0.5 no tiene vulnerabilidades pre-autenticación conocidas graves; OpenSSH 8.2p1 tampoco presenta vulnerabilidades críticas sin credenciales).

### Prioridad Baja:
5. **Revisión exhaustiva del archivo `crew.jpg`:**
   - Realizar esteganografía básica (strings, binwalk, steghide) para descartar datos ocultos.

---

**Nota final:** La exploración no reveló flags ni vectores de ataque evidentes más allá del acceso anónimo restringido en FTP. Se recomienda priorizar la explotación de FTP con paths conocidos y fuzzing más profundo en HTTP.