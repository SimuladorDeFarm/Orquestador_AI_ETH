Aquí está el reporte ejecutivo final de la fase de exploración, sintetizando todos los hallazgos de las tres iteraciones.

---

## Reporte Ejecutivo Final - Fase de Exploración

**Fecha del reporte:** [Fecha de la exploración]
**Objetivo:** 10.65.172.198
**Sistema Operativo:** No detectado (por política)

### 1. Servicios Descubiertos

| Puerto | Protocolo | Servicio | Versión / Notas |
| :--- | :--- | :--- | :--- |
| 80/tcp | HTTP | Apache (inferido) | - |

Únicamente se encontró un servicio HTTP en el puerto 80. No se detectaron otros puertos o servicios activos.

### 2. Rutas y Archivos Expuestos

#### Rutas Principales
- `/` (Página principal: "Corridor")

#### Rutas de Hashes MD5 (Todas llevan al mismo contenido)
- `/c4ca4238a0b923820dcc509a6f75849b`
- `/c81e728d9d4c2f636f067f89cc14862c`
- `/eccbc87e4b5ce2fe28308fd9f2a7baf3`
- `/a87ff679a2f3e71d9181a67b7542122c`
- `/e4da3b7fbbce2345d7772b0674a318d5`
- `/1679091c5a880faf6fb5e6087eb1b2dc`
- `/8f14e45fceea167a5a36dedd4bea2543`
- `/c9f0f895fb98ab9159f51fd0297e236d`
- `/45c48cce2e2d7fbdea1afc51c7c6ad26`
- `/d3d9446802a44259755d38e6d163e820`
- `/6512bd43d9caa6e02c990b0a82652dca`
- `/c20ad4d76fe97759aa27a0c99bff6710`
- `/c51ce410c124a10e0db5e4b97fc2af39`

#### Archivos Estáticos
- `/static/css/main.css`
- `/static/img/empty_room.jpg`

#### Rutas No Encontradas (Resultados de `ffuf`)
- `/static/` → 404 Not Found
- `/static/img/` → 404 Not Found
- No se encontraron las rutas `/admin`, `/backup`, `/robots.txt`, `/flag` ni ninguna otra ruta relevante durante la enumeración con `ffuf` y la wordlist `/home/ubuntu/common.txt`.

### 3. Contenido Relevante de Páginas

- **Página Principal (`/`):**
    - Título: "Corridor".
    - Contenido: Una imagen de un pasillo (corredor) con 13 áreas cliqueables.
    - Cada área cliqueable enlaza a una de las rutas de hash MD5 listadas anteriormente.
    - Los hashes corresponden a los números del 1 al 13.

- **Rutas de Hashes (ej. `/c4ca4238a0b923820dcc509a6f75849b`):**
    - Todas las 13 rutas devuelven exactamente el mismo contenido: una página que muestra una imagen de fondo llamada `empty_room.jpg` (una habitación vacía).
    - No se encontró ninguna diferencia entre estas páginas.

- **Archivo `/static/css/main.css`:**
    - Contiene únicamente reglas de estilo CSS básicas.
    - No se encontraron comentarios ocultos, metadatos, ni información sensible en este archivo.

### 4. Flags Encontradas

**NINGUNA.** No se encontraron flags (en formato `flag{...}` o similar) durante ninguna de las iteraciones de exploración en las páginas, rutas, archivos estáticos, ni en el código fuente.

### 5. Vectores de Ataque Sugeridos para la Siguiente Fase

1.  **Análisis de Imágenes:** Se recomienda realizar un análisis de las imágenes descargadas (`empty_room.jpg` y la imagen del pasillo en la página principal). Utilizar herramientas como `steghide`, `strings`, `binwalk` o `exiftool` para verificar si contienen datos ocultos (esteganografía) o metadatos relevantes.
2.  **Fuerza Bruta de Directorios con Otras Wordlists:** Repetir la enumeración de directorios con una wordlist más grande y específica (como `directory-list-2.3-medium.txt` o `raft-large-words-lowercase.txt`) para intentar descubrir rutas que la wordlist `common.txt` no encontró, ya que la superficie de ataque parece muy limitada.
3.  **Análisis de la Aplicación Web para Vulnerabilidades de Tipo "Business Logic":** Dado que las 13 rutas de Hash son puertas "decorativas" que llevan al mismo lugar, podría haber una lógica de negocio o secuencia a seguir. Probar a abrir las puertas en un orden específico (1-13, 13-1, etc.) o buscar una ruta que tome un parámetro (ej. `/?door=5`).